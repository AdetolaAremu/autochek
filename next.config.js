/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "storage.googleapis.com",
      "media.autochek.africa",
      "unsplash.com",
    ],
  },
};

module.exports = nextConfig;
